----frontend
creating routes was the 1st issue I faced I need to work on the react-router-dom:-
solution:- wee simple create the link tag in place of anchor tag and use the attribute like path=/' in the place where we want the button to show but it doesn't work untill.
in the app or whereever we want the route to print out/show the component there we use router routes in routes use the attribute of to='/' element={<home/>}